<div id="<%= dom_id video %>">
  <p>
    <strong>Title:</strong>
    <%= video.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= video.description %>
  </p>

  <% if video.video.attached? %>
    <%= video_tag url_for(video.video), controls: true %>
  <%end%>

</div>
